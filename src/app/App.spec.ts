import 'react-native';
// import React from 'react';
//import AppSample from '../AppSample';

// Note: test renderer must be required after react-native.
// import renderer from 'react-test-renderer';

describe('app', () => {
  it('renders correctly', () => {
    //renderer.create(<AppSample />);
  });
});
